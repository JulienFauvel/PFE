{% extends 'admin/admin.html.twig' %}

{% block content %}
    <h4>Number of users : {{ users|length }}</h4>
    {% for user in users %}
        <div>
            <p>{{ dump( user ) }}</p>
        </div>
    {% endfor %}
{% endblock %}